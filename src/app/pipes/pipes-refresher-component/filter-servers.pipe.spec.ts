import { FilterServersPipe } from './filter-servers.pipe';

describe('FilterServersPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterServersPipe();
    expect(pipe).toBeTruthy();
  });
});
