import { EvenBetterHighlighterDirective } from './even-better-highlighter.directive';

describe('EvenBetterHighlighterDirective', () => {
  it('should create an instance', () => {
    const directive = new EvenBetterHighlighterDirective();
    expect(directive).toBeTruthy();
  });
});
