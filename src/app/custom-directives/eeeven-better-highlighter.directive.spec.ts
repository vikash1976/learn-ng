import { EeevenBetterHighlighterDirective } from './eeeven-better-highlighter.directive';

describe('EeevenBetterHighlighterDirective', () => {
  it('should create an instance', () => {
    const directive = new EeevenBetterHighlighterDirective();
    expect(directive).toBeTruthy();
  });
});
